default:
  image: ruby:2.7
  before_script: bundle install

pages:
  stage: deploy
  script:
    - bundle exec middleman build
    - mv build public
  artifacts:
    paths:
      - public
  only:
    - master
